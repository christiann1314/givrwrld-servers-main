# GIVRwrld API – copy to .env and fill in values.
# Required for DB: MYSQL_*, JWT_SECRET. For checkout: PAYPAL_*. For provisioning: PANEL_*.

# Server
PORT=3001
NODE_ENV=development

# MySQL / MariaDB (must match your DB; for Docker see docs/LOCAL-DB-DOCKER.md)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=app_rw
MYSQL_PASSWORD=devpass
MYSQL_DATABASE=app_core

# Auth (required)
JWT_SECRET=local-dev-jwt-secret-change-in-production

# PayPal (for checkout)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_SANDBOX=true

# Pterodactyl (for server provisioning)
PANEL_URL=
PANEL_APP_KEY=

# Frontend (CORS, redirects)
FRONTEND_URL=http://localhost:8080
PUBLIC_SITE_URL=http://localhost:8080
